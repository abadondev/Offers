import { SetNavbarDirective } from './set-navbar.directive';

describe('SetNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new SetNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
